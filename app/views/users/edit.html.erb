<h1><%= @user.username %>'s setting page</h1>

<h1>Profile Setting</h1>
<%= form_with(model: @user) do |f| %>
  <%= f.file_field :profile_picture %>
  <%= f.submit 'Save' %>
<% end %>

<h1>Account Setting</h1>
<%= render partial: 'devise/registrations/edit' %>