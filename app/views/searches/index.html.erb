<h1>This is your Search Page</h1>

<%= form_with(url: search_path, method: :get) do |f| %>
  <%= text_field_tag :search %>
  <%= f.submit 'search' %>
<% end %>

<% @users.each do |user| %>
  <%= link_to "#{user.username}", user_path(user) %>
<% end %>